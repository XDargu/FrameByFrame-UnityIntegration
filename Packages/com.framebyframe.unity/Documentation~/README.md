# Frame by Frame Unity Integration

This package contains the Unity-side websocket bridge and built-in recorder components for Frame by Frame.

Open **Window > Frame by Frame** in Unity to view connection status, start or stop the local websocket server, toggle recording options, and control raw recording.

The package currently starts the editor websocket service automatically and keeps it available across play-mode transitions.

